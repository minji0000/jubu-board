<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/xml/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jubu.mapper.BoardMapper">

    <insert id="insert">
        INSERT INTO board (writer_id, title, content)
        VALUES (#{writerId}, #{title}, #{content})
    </insert>

    <select id="getList" resultType="com.jubu.domain.BoardVO">
        SELECT * FROM board ORDER BY board_id DESC
    </select>

</mapper>